-- ============================================
-- مایگریشن ۰۰۲۴ - اضافه کردن ستون‌های کاربر به notification_logs
-- ============================================
-- تاریخ: ۱۴۰۵/۰۵/۲۱
-- توضیح: اضافه کردن ستون‌های user_id و is_user_notification به جدول notification_logs
--        برای پشتیبانی از اعلان‌های کاربران در سیستم تلگرام
-- ============================================

PRAGMA foreign_keys = ON;

-- ============================================
-- 1. اضافه کردن ستون user_id
-- ============================================
ALTER TABLE notification_logs ADD COLUMN user_id INTEGER;

-- ============================================
-- 2. اضافه کردن ستون is_user_notification
-- ============================================
ALTER TABLE notification_logs ADD COLUMN is_user_notification INTEGER DEFAULT 0;

-- ============================================
-- 3. ایجاد ایندکس برای بهبود عملکرد
-- ============================================
CREATE INDEX IF NOT EXISTS idx_notification_logs_user_id ON notification_logs(user_id);

-- ============================================
-- پایان مایگریشن
-- ============================================