// Versioned public API for Website and mobile clients.
// The legacy /api/products route remains available and uses the same shared service.
export { onRequestGet, onRequestOptions } from "../products";
