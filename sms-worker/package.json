{
  "name": "sms-gateway-worker",
  "version": "1.0.0",
  "description": "SMS Gateway Worker for OpenWrt + X28",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["sms", "gateway", "cloudflare", "worker"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}